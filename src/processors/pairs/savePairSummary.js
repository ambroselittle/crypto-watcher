module.exports = {
    process: async (data, context) => {
        console.log('TODO: Save Pair Summary');
        // save stdDev and (if/when we cache it, rank)

        return { data, context };
    }
}